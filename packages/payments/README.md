# @repo/payments

Bounded context financiero DNX (contratos + reglas de dominio puras).

- **Etapa 02:** diseño — sin HTTP a Mercado Pago, sin Prisma, sin cutover CLF.
- Docs: `docs/dnx-payments/`.

```bash
pnpm --filter @repo/payments test
pnpm --filter @repo/payments check-types
```
