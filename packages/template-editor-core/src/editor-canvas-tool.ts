/** Herramienta activa en el lienzo del editor de plantillas V2 (atajos T / V / H). */
export type TemplateEditorCanvasTool = "select" | "text" | "hand";

/** Al resetear zoom (⌘0), el lienzo también vuelve el scroll del área de trabajo al origen. */
export const TEMPLATE_V2_RESET_WORK_SCROLL_EVENT = "template-v2-editor-reset-work-scroll";
