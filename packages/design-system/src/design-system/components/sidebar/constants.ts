/** Ancho por defecto del panel lateral (px). */
export const SIDEBAR_WIDTH_PX = 280;

/** Breakpoint móvil / tablet para drawer (alineado a convención lg). */
export const SIDEBAR_MOBILE_MEDIA = "(max-width: 1023px)";
