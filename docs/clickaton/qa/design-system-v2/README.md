# QA visual — Design System V2

Capturas generadas en Etapa 06 (Playwright Chromium).

Viewports: desktop 1440×1000 · tablet 768×1024 · mobile 390×844

Rutas: `/`, `/maratones`, `/maratones/demo`, `/como-funciona`, `/comunidad`, `/organizar`, `/sponsors`, `/nosotros`, `/contacto`, `/design-system`.

Estas PNG son artefactos locales de auditoría; no son assets de producto.
