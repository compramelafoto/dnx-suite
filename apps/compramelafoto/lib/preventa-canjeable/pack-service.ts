/** Stub Block A — catálogo preventa se importa en oleada comercial. */
export async function listActivePacksForPublicCatalog(
  _albumId: number,
  _now?: Date,
  _opts?: { includeScheduled?: boolean; hasPhotos?: boolean },
): Promise<unknown[]> {
  return [];
}
