/**
 * Contrato de versiones persistidas en `DesignRevision.dataJson` y preflight.
 * Mantener alineado con `types.ts` y `build-preflight.ts`.
 */
export {
  SCHOOL_DESIGN_REVISION_SCHEMA_VERSION,
  SCHOOL_PREFLIGHT_SCHEMA_VERSION,
} from "./types";
