/** Re-export — cookie helpers viven en lib/auth (login unificado). */
export {
  SESSION_COOKIE_OPTIONS,
  attachClickatonSessionCookieToResponse,
  cookieOptionsForRequest,
  getDefaultSessionCookieOptions,
} from "@/lib/auth/session-cookie";
