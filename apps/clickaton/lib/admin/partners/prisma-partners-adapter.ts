/**
 * Thin re-export — implementación canónica en @repo/db.
 * Clickatón y FotoRank comparten el mismo Prisma PartnersRepository.
 */
export { createPrismaPartnersRepository } from "@repo/db/partners-prisma-repository";
