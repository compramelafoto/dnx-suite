/**
 * Versión del pipeline de render de placas Clickatón.
 *
 * Incrementar cuando cambie de forma material el output PNG para el mismo input
 * (p. ej. motor Playwright, fuentes embebidas, antialiasing, escala DPI).
 * No hace falta bump por cambios solo de persistencia o HTTP.
 */
export const CLICKATON_CARD_RENDERER_VERSION = "template-engine-renderer:1";
