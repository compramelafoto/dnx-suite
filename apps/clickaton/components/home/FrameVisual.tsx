/**
 * Reexport del visor — mantiene compatibilidad con imports Etapa 01.
 * Preferir `ViewfinderFrame` en código nuevo.
 */
export { ViewfinderFrame as FrameVisual } from "@/components/brand/ViewfinderFrame";
