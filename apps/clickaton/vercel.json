{
  "installCommand": "cd ../.. && pnpm install",
  "buildCommand": "cd ../.. && pnpm --filter clickaton build",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/expire-registration-holds",
      "schedule": "*/15 * * * *"
    }
  ]
}
