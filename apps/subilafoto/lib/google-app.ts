/**
 * Identificador de esta app dentro del transporte OAuth compartido.
 *
 * Vive en su propio archivo porque un `route.ts` de Next sólo puede exportar
 * los nombres que el framework reconoce; cualquier otro export rompe el build.
 */
export const APP_OAUTH = "subilafoto";
